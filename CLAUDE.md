# CLAUDE.md — Rules for this project

## Hard rules — never do these without explicit user confirmation

1. **Never run `git commit` or `git push`** — always show the user what would be committed and let them run it manually.

2. **Never run `npm publish`** — always stop and tell the user to run it themselves. Publishing to npm is irreversible and must be a deliberate human action.
